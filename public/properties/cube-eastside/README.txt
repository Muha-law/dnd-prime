DROP THE CLIENT'S 14 PHOTOS HERE.

Name them 01 through 14. Photo 01 is the cover (shown on the Properties
grid and as the large lead image on the detail page). The rest appear in
the gallery in number order.

The site currently points at .svg placeholders. Once your real photos are
in this folder as JPGs, open src/data/properties.js and change the file
extensions for this listing from .svg to .jpg (find "cube-eastside" — there
are 15 references: 1 cover + 14 images). Or rename your files to .svg, but
.jpg is correct for photos.

Recommended: resize longest edge to ~1600px and compress so each is
under ~300kb, so the gallery loads fast.
